apiVersion: v1
kind: Service
metadata:
  name: scaletestapp
  labels:
    app: scaletestapp
spec:
  ports:
    - name: http
      targetPort: 8080
      port: 80
  selector:
    app: scaletestapp
  type: NodePort
